apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grm-postgres-pvc
  namespace: grm
  labels:
    app.kubernetes.io/name: grm
    app.kubernetes.io/component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
